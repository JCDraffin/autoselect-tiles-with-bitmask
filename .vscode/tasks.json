{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "dub",
            "run": true,
            "compiler": "dmd",
            "archType": "x86_64",
            "buildType": "debug",
            "configuration": "application",
            "_generated": true,
            "problemMatcher": [
                "$dmd"
            ],
            "group": "build",
            "label": "dub-auto: Build tile-bitmasking-algorithm",
            "detail": "dub build --compiler=dmd -a=x86_64 -b=debug -g -unittest -w -c=application"
        }
    ]
}